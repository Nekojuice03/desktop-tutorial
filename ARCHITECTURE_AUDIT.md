# 架構缺陷盤點與修正紀錄（2026-09-07）

定位轉為「數位孿生時效性感知 + 單跳基礎設施中繼恢復」之後，對整體架構做的
一次全面體檢。分四類：**已修**、**待修**、**須聲明的限制**、**實驗待補**。

判準：一項若會讓口試委員問出「那你這個貢獻不就是免費的／不就是假的」，
列為必修；若只是模型簡化且學界慣例接受，列為須聲明。

---

## 一、已修（本次）

### A1. 中繼那一跳是免費的 ★核心貢獻零成本
恢復機制是本研究的貢獻，但 RSU↔RSU 的有線骨幹被寫成零成本
（原註解：「RSU 間走有線骨幹，忽略」）。`recovery="v2i"` 因此**必勝**，
消融沒有資訊量。

`nodes.py:126` 早已註冊一條 100 Mbps、有 FIFO 排隊的 `backhaul` 共享鏈路，
但只有雲端在用。三條恢復路徑改為共用同一條鏈路（新增 `_backhaul_hop()`）：

| 路徑 | 修正前 | 修正後 |
|---|---|---|
| V2I 遷移 | `RSU_EXTRA + t_up + t_dn` | `2·RSU_EXTRA + t_up + **骨幹排隊+傳輸** + t_dn` |
| RSU 換手 | `RSU_EXTRA + t_dn` | `RSU_EXTRA + **骨幹** + t_dn` |
| 抵達補送 | `RSU_EXTRA + tx` | `n·RSU_EXTRA + **骨幹** + tx`（V2V 時 n=2） |

### A2. 遷移只收一次基站存取，實際有兩次
V2I 遷移含兩段無線（執行車→RSU、RSU→持有車），各需一次 Uu 存取，
原本只收一次 20 ms。已改為 `2 × RSU_EXTRA_LATENCY`。

### A3. 恢復路徑的下行能耗用錯發射端功率
`nodes.estimate()` 的下行依發射端選功率（RSU 2.0 W / 車 0.5 W），但三條
恢復路徑一律用車輛的 `TX_POWER_W`，低估了路側下行能耗。已對齊。

### A4. `kalman` 與 `route` 是互斥分支，無法組合
`_pair_contact()` 走 `kalman` 就直接 return，永遠碰不到路線資訊；走 `route`
則沒有 EKF。「軌跡預測 + 路線意圖」這個組合**先前不存在**。

新增 `predictor="kalman+route"`：先 EKF-CTRV 前推（含 τ 補償）得連線壽命，
再取路線分歧／離場時間的下限。追蹤器的餵食條件同步放寬（原本只在
`predictor == "kalman"` 時更新，否則永遠冷啟動）。

### A5. 斷線歸因缺乏儀器
原本只有 `link_break` 一個總數。新增四類互斥計數：
`break_consumer_left` / `break_helper_left` / `break_turn` / `break_straight`
（航向變化門檻 `TURN_HEADING_DEG = 30°`），另加 `kf_cold`（EKF 未熱身而
退回等速外推的次數）。

---

## 二、待修（依優先序）

### B1. 骨幹敏感度掃描 ★補完 A1 才有的新結果
`BACKHAUL_CAPACITY_BPS`（100 Mbps）與 `CLOUD_EXTRA_LATENCY`（40 ms）目前是
固定值。應掃描出**中繼從划算變成不划算的交叉點**——這比「中繼比不中繼好」
有價值得多，也是回答「什麼條件下該中繼」的唯一方式。

### B2. 中繼失敗條件只有覆蓋空洞
補完 A1 後應該多一種失敗來源：骨幹壅塞導致補送超時。目前壅塞只會讓延遲
上升，不會獨立計數。

### B3. 沒有多跳對照，「單跳」站不住
賣點是**單跳**基礎設施中繼，但沒有多跳基線（例如兩跳 V2V 接力）。多跳中繼
是文獻既有做法，缺對照就無法論證「單跳為何足夠」。

### B4. 能耗／延遲是「完成任務」的平均，跨 recovery 設定不可直接比較
`avg_energy_j` 的分母是 `energy_n`（完成筆數）。開啟恢復後被救回的任務會
進入分母，實測 `v2i` 的平均能耗反而**低於** `fail`（5.38 vs 5.41 J）——這是
分母改變造成的假象，不是恢復比較省電。論文若要比能耗，必須改成
**每產生任務的總能耗**，或明確標註口徑。

### B5. `arrival_delivery` 主實驗預設關閉
`vec_env_ma.py` 建構子預設 `False`，只有消融與白箱測試會開。要決定主結果
是否納入；納入的話論文得說明它是選項。

### B6. `rsu_handover` 不計入 `link_break`
程式碼註解明講換手是「蜂巢式常態操作，不算斷線」。把它與 V2I 遷移、抵達
補送並列為「中斷恢復三機制」語意上偏寬。建議統稱改為
**「結果回傳失效之恢復」**，可同時涵蓋斷線與覆蓋轉移。

---

## 三、須聲明的限制（不修，寫進論文限制節）

### C1. 通道完全確定性
無隨機陰影／快衰落（`COMM_MODEL.md` 開頭已聲明，為 RL 收斂與可重現）。
**建議補一組對數常態陰影（σ = 4–8 dB）的敏感度實驗，只在評估階段開、
訓練仍用確定性**——約 30 行，能擋掉「你的通道太乾淨」。

不建議為此導入 ns-3 / Veins：它補的是無線 MAC 層，而本研究的弱點在有線
骨幹（A1），導入後那一跳照樣免費；且 SUMO+ns-3 耦合本身就是一篇論文的
工作量，會讓現有 MAPPO 訓練管線全部重做。

### C2. 無 MAC 競爭／碰撞，以固定存取延遲近似
PC5 5 ms、Uu 20 ms。碩論用解析鏈路預算是常態。

### C3. `route` predictor 是 oracle
直讀 SUMO 真值路線，不受 τ 影響（`run_dt_delay.py:31` 已註明不掃 τ）。
只能當**上界對照**，不能當方法賣。

### C4. 孿生的不完美只有「延遲」一種
沒有量測雜訊、封包遺失、更新率不齊。τ 是唯一的不完美來源。

### C5. MockWorld 不能支撐任何移動性宣稱
單向走廊、y 固定、角度只有 90/270、車輛撞邊界折返且永不離場。因此：
- ω ≡ 0，CTRV 退化為等速模型
- `consumer_left` 恆為 0，抵達補送不會觸發
- `break_turn` 計到的是端點折返，不是路口轉向

**所有移動性相關結論必須來自 SUMO。** 白箱測試（`verify_invariants.py` §6）
已單獨驗證 EKF 的 ω 估計收斂（ω̂=0.200）與轉彎預判有效（直行 60 s vs
轉彎 9 s），機制本身是對的——但**評估環境沒有把它逼出來**。

---

## 四、實驗待補

1. **SUMO 版斷線歸因表**（A5 的計數器已就位）—— 決定第 4.7 節怎麼寫，
   也是回答「你到底在解什麼問題」的唯一硬證據
2. **骨幹敏感度交叉點**（B1）
3. **陰影衰落敏感度**（C1）
4. **多跳對照**（B3）
5. 主結果、消融、種子、τ 掃描全部重跑（A1–A3 改變了成本模型）

---

## 五、現有實測（mock、greedy、4 回合、τ=4）

修正後：

| predictor | recovery | 成功% | 延遲ms | 能耗J | 斷線 | 救回 | 損失 | 冷啟 |
|---|---|---|---|---|---|---|---|---|
| linear | fail | 94.4 | 395 | 5.43 | 37 | 0 | 37 | 0 |
| linear | v2i | 94.8 | 394 | 5.40 | 37 | 37 | 0 | 0 |
| kalman | fail | 94.8 | 393 | 5.41 | 35 | 0 | 35 | 376 |
| kalman | v2i | 95.3 | 393 | 5.38 | 35 | 35 | 0 | 376 |
| kalman+route | fail | 94.8 | 393 | 5.41 | 35 | 0 | 35 | 376 |
| kalman+route | v2i | 95.3 | 393 | 5.38 | 35 | 35 | 0 | 376 |

`kalman+route` 在 mock 等同 `kalman`（mock 無路線資訊，`route_divergence_time`
回 None）——**這是預期行為**，差異只會在 SUMO 出現。

斷線歸因（τ=8）：40 次斷線中僅 6 次歸因轉向，且那 6 次是 mock 端點折返。
`kalman` 相對 `linear` 斷線數幾乎不變（40→35），成功率增益幾乎全部來自
`stale_miss` 263→0 —— **現有實驗證明的是時效補償，不是轉向處理**，這與
「Staleness-Aware」的定位一致。
